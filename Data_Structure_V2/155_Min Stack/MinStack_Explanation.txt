আপনার আসল লক্ষ্য হলো এমন একটি **Stack** বানানো যেখানে শুধু `Push`, `Pop`, `Top` ই না, বরং **যেকোনো সময় মিনিমাম ভ্যালু O(1) টাইমে বের করা যাবে**।  

## 🟢 Efficient সমাধান — Two Stack Approach  
আমরা ২টা stack ব্যবহার করি:  
1. **stack** → normal ভ্যালু রাখবে  
2. **minStack** → প্রতিবারের জন্য বর্তমান মিনিমাম ভ্যালু track করবে  

---

### 🔹 Push(val)
- প্রথমে `stack.Push(val)` করি (ভ্যালু main stack-এ ঢুকলো)  
- তারপর `minStack` দেখি:  
  - যদি `minStack` ফাঁকা থাকে বা `val <= minStack.Peek()` হয়, তাহলে `minStack.Push(val)`  

👉 এর মানে, `minStack` সবসময় এখন পর্যন্ত ছোট ভ্যালুগুলো ধরে রাখবে।  

---

🔹 Pop()
- `stack.Pop()` করি  
- যদি pop হওয়া ভ্যালুটা `minStack.Peek()` এর সমান হয়, তাহলে `minStack.Pop()` ও করি  
👉 কারণ ওই ভ্যালু মিনিমাম ছিল, তাই তাকে `minStack` থেকেও বের করতে হবে।  

---

🔹 Top()
- `stack.Peek()` রিটার্ন করি → এটাই উপরের ভ্যালু  

---

🔹 GetMin()
- `minStack.Peek()` রিটার্ন করি → কারণ `minStack` সবসময় বর্তমান মিনিমাম ধরে রাখছে।  

